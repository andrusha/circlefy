<?php
    $_h = array(
        'title'   => 'homepage',
        'scripts' => array('main.js', 'push.js', 'search.js', 'modal.js', 'feed.js', 
            'sidebar.js', 'Swiff.Uploader.js', 'user_edit.js', 'group_create.js', 'notification.js', 'post.js'),
        'styles'  => array('roar.css', 'modal.css'),
        'vars'    => array('user'  => $me,
                           'guest' => $guest,
                           'feed'  => array('type' => $feed_type, 
                                            'id' => null, 
                                            'inside' => ($feed_type == 'feed' ? 1 : 0)) 
                            ),
        'guest'   => $guest);
    include(HEADER);
?>
    
    <div id="content">

        <div id="left">
            <?php
                $_f = array(
                    'feed' => $feed,
                    'name' => $feed_name,
                    'show_inside' => $feed_type == 'feed'
                );
                include(FEED);
            ?>        
        </div><!--left-->
        
        <?php
            $_s = array(
                'circles'   => $circles,
                'show_nav'  => true,
                'feed_type' => $feed_type
            );
            include(SIDEBAR);
        ?>
        
    </div><!--content-->

<?php
    include(FOOTER);
?>

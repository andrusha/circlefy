<!doctype html>
<html>
<head>
	<? 
	$tpl_title = "Channel Management";
	include(OK_HEADER);
	?>
	<script type="text/javascript" src="/views/scripts/tap.js"></script>
	<script type="text/javascript" src="/views/scripts/groups.js"></script>
	<script type="text/javascript" src="/views/scripts/search.js"></script>
</head>
<body>
	<div id="wrapper">

	<? include(OK_TOPBAR); ?> 

	<div id="main">
		
		<? ob_start(); ?>

				<div id="lists">
					<h2>
						<span id="tab-name">channels</span>
					</h2>
					<ul class="panel selected">
						<li class="panel-item" >
							<span class="count"></span>
							<a href="/create_channel" class="names inblock ellipsis">create new channel</a>
						</li>
						<li class="panel-item" >
							<span class="count"></span>
							<a href="/invite" class="names inblock ellipsis">invite people</a>
						</li>
					</ul>
				</div>
		<? $add_sidebar = ob_get_clean(); ?>	
		<? include(OK_SIDEBAR); ?>

			<div id="content">
				
				<div class="section">
					<div class="header header-non">
						<a id="newtaps" href="" class="floater"></a>
						<h2 class="header-title">
							<img src="/images/flat/loader.gif" />
							<span class="title">channels</span>
						</h2>
						<span class="stream-name">Your Channels <a href="/create_channel">create channel</a></span>
					</div>
					<div class="body">
						<ul class="bodylist">
							<?php	if($group_results != NULL):
								foreach ($group_results as $group):
									$gid = $group['gid'];
									$gname = $group['gname'];
									$admin = $group['admin'];
									$pic = $group['pic'];
									$type = $group['type'];
									$size = $group['size'];
									$official = $group['official'];
									$focus = $group['focus'];
									$descr = $group['descr'];
									$count = $group['count'];
									$last_chat = $group['last_chat'];
									$last_user = $group['last_uname'];
									$symbol = $group['symbol'];
							?>
							<li id="gid_<?= $group['gid'] ?>" class="container" 
								data-id="<?= $group['gid'] ?>" data-symbol="<?= $group['symbol'] ?>">
								<button class="invite">invite</button>
								<img src="/group_pics/<?= $group['pic'] ?>" class="avatar" />
								<h2>
									<a href="/channel/<?= $group['symbol'] ?>" title="<?= $group['gname'] ?>"><?= $group['gname'] ?></a>
									<span class="nick">
										<? if ($group['admin']): ?>
										<a href="/channel_edit?channel=<?= $group['symbol'] ?>" class="follow_button gm">Manage</a>
										<? endif; ?>
										<a href="#" class="leave follow_button gm">Leave</a>
									</span>
								</h2>
								<p class="headline">
                                    <? if ($group['descr'])
                                        echo "&#8220;".$group['descr']."&#8221;";
                                       else
                                        echo "There is no description yet";
                                    ?></p>
								<p class="stats">
									<? if ($official == "*"): ?>
									<strong>Official Channel.</strong>
									<? endif; ?>
									<strong><?= $group['size'] ?></strong> Members joined, 
									<strong><?= $group['count'] ?></strong> Taps sent, and
									<strong><?= $group['responses'] ?></strong> Responses received. 
								</p>
							</li>
							<?
								endforeach;
								else:
							?>
							<li class="notice notice-group">
                                                                <p>You are in no channels!  Search above to find them!</p>
                                                        </li>
                                                        <?php endif; ?>

						</ul>
					</div>
				</div>

			</div>

		</div>
	</div>
    <?include(OK_FOOTER)?>
	<?include(JAVASCRIPT_TEMPLATES)?>	
</body>
</html>

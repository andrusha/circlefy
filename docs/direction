1) You need to run:

mysql -uxxxx -pxxxx
CREATE DATABASE rewrite;
use rewrite;
source init.sql;

2) Setup your config in the root of the code base ( there is only config and index.php , so it's config )
3) install meteor using the Config.pm from this directory
4) INSTALL MEMCACHE php module and server
